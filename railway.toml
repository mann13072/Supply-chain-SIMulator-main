[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install -r backend/requirements.txt && uvicorn backend.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
